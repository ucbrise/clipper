#include <utility>

const size_t FIXED_BATCH_SIZE = 1;
const bool IGNORE_OVERDUE_TASKS = true;
const bool USE_FIXED_BATCH_SIZE = true;
static constexpr double LATENCY_CUTOFF_PERCENTAGE = 0.8;
const bool USE_LATENCY_CUTOFF = false;